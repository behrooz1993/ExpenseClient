import ValueKeeper from './ValueKeeper';

const BASE_URL = "http://localhost:8080/api/v1";

const ApiHelper = {
    registerUser(firstName,lastName,cellphone,callback) {
        const fetchData = {
            method: "POST",
            body: ""
        }
    }
};

export default ApiHelper;

